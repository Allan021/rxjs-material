import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    display: "flex",
    width: "100%",
  },

  pokemonMiniCard: {
    display: "flex",
    justifyContent: "space-around",
    alignItems: "center",
  },
});
